# Fetched repository inspection

Baseline: `58987d2` on existing main, fetched from the user-specified origin.
The initial empty-checkout observation is superseded for research purposes, not
erased. A local recovery bundle retains the preliminary unrelated root commit;
new research is committed directly on the fetched history, with no merge of that
root and no new branch. All original tracked files were restored unchanged.

Read: root AGENTS.md; README; programme ROADMAP and ARCHITECTURE; North Star;
substrate v1.1 §§9–10; finite causal identification §3; longitudinal kernel fibres
§§1–3; finite causal verification README and kernel-fibre implementation profile.

Findings:
- The north star explicitly allows several mathematical structures and established
  results. Current organization need not be adopted as a universal foundation.
- Substrate §10.2 already uses an exact compatible-global-model set and distinguishes
  it from outer relaxations. §9 already requires intervention-preserving maps.
- The one-stage response-type fibre retains joint potential outcomes and can encode
  dependence; it deliberately rejects unsupported independence restrictions.
- The longitudinal kernel fibre is a saturated Cartesian completion, expressly not
  a general SCM fibre. Its independent missing coordinates are a premise of that
  profile, not a general law about uncertainty.
- Current engineering contracts and certificate machinery are useful examples but
  do not determine the comparative research answer. No modifications to old code
  or replay evidence are required for this new mathematical investigation.
- The roadmap's default next measurement/strategic work is not the user's new task.
  Record a distinct foundation reassessment and update living navigation after its
  bounded result closes. Do not reopen KL5/KL6 or rewrite frozen editions.

Research implication: test a mechanism-interface representation which can retain
coupled alternatives explicitly. Compare it to the existing saturated profile;
there is no warrant to replace that valid specialized result.
