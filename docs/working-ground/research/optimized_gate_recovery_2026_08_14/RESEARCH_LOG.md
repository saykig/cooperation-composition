# Research log

## 14 August 2026 — G01: scope and first failed route

Read governing instructions, skill, programme and predecessor records. Preserve
unrelated foundations README edits and untracked foundations AGENTS.md. Work on main.

Rejected direct information-driven switch at a fixed fine in the inherited
fully informed, public, state-only payoff model: positive pre-message support
lets sender trembles generate the same certificate posterior simplex. Merely
changing a prior or noisy source cannot remove a continuation from that simplex.
This is an obstruction to that construction, not a proof of all optimized value
continuity with multiple receivers.

The prior payoff-delta example is outside the requested information-only layer.
Do not recycle it as a fixed-payoff counterexample. User authorized analysing both
sender information partitions and explicitly labelling the change.

## 14 August 2026 — G02: candidate fixed-payoff witness

A coarsely informed sender cannot change the relative likelihood of states 1 and 2
by a tremble at one information set. A source perturbation therefore selects a
receiver best response even though posterior probabilities stay bounded away from
zero. Gate optimization over a compact two-dimensional noisy binary-channel menu
cannot eliminate this perturbation. Candidate value: 0 at zero, 1+t/2 otherwise.
This defeats weak existence, not a hypothesis requiring near-optimal recovery.
Analytical construction precedes executable search. Exact checks still pending.

## 14 August 2026 — G03: exact result and interpretation audit

Derived C1–C3 over the full two-dimensional gate menu, including exclusion of
all mixed responses below the threshold. Source family and all posterior support
bounds are uniform. Confirmed V=0 when the sender is fully informed, when a zero
contrast gate is admitted, or when weak PBE allows arbitrary off-path beliefs.
The last control exposed the need to state sequential consistency explicitly;
no failed claim of a weak-PBE counterexample is retained as a result.

Derived F1/F2 and R1–R3. General continuity is established recovery mathematics;
one-receiver upward closure gives primitive sufficient conditions. A finite-cover
argument preserves one institution design while varying law-specific witnesses.

Exact search and independent original-cell arithmetic: 2,025 law/gate checks,
45 gates, five radii, nine laws each. Normal/optimized receiver outputs coincide;
wrong-value and stale-identity negative controls reject. No numeric solver or
formalization required. Source reading used a directly available July 2025 signaling
draft; January 2026 Drive text was not accessible. PWI text was inspected through
the web tool, but direct byte retrieval returned HTTP 406; no hash is fabricated.

## 14 August 2026 — G04: close the bounded goal

Mathematical and evidence milestone `e2daee7` committed/pushed after activation
`a3bb951`. Author audit checks off-path consistency, mixed-response exclusion,
attainment, common-design quantifiers, uniform support and the logical persistence
tension. No mathematical correction to that edition was required. Programme
steering now closes the noisy-gate question at the explicit recovery boundary.
Preserve current foundations local changes and all prior frozen records. Retain
release note in the repository only; no GitHub Release, Writ or Decision Lab changes.
